import { MycarousalDirective } from './mycarousal.directive';

describe('MycarousalDirective', () => {
  it('should create an instance', () => {
    const directive = new MycarousalDirective();
    expect(directive).toBeTruthy();
  });
});
